import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,// 하나의 컴포넌트만 render할 수 있다 ! //추가적인 컴포넌트는 App밑에 둔다
  document.getElementById('root')
);
